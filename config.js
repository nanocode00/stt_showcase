window.MALLO_SHOWCASE_CONFIG = Object.freeze({
  // 공개 backend가 정해지면 HTTPS origin만 입력합니다. 끝의 /는 생략합니다.
  API_BASE_URL: "https://murphy-belongs-behavioral-minimize.trycloudflare.com",
  REQUEST_TIMEOUT_MS: 90000,
  MAX_RECORDING_SEC: 20,
});
